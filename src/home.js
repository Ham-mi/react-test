import React from 'react';
import {
  StyleSheet,
  Text,
  View,
  Image,
  TouchableOpacity
} from 'react-native';

const home = ({navigation}) => {
    return(
        <View>
            <Text>Ham</Text>
        </View>
    );
};

const styles = StyleSheet.create({

});


export default home;